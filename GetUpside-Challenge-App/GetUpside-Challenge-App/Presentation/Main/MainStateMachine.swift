import Foundation

extension Main {
    final class StateMachine {
        enum State {
            case idle
            case loading
            case operating
            case error
        }
        
        enum Event {
            
        }
    }
}
